# Matchmaker

Simple queue-based matchmaker

# Try it

1. Download (Clone):

```shell
git clone git@github.com:jorahty/matchmaker.git
```

2. Install dependencies:

```shell
npm install
```

3. Start server:

```shell
npm start
```
